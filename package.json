{
  "name": "steamoperationhub-releases",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "npm run indexes:generate",
    "check": "npm run indexes:check",
    "indexes:generate": "tsx scripts/release-news/generate-indexes.ts",
    "indexes:check": "tsx scripts/release-news/generate-indexes.ts --check",
    "test": "tsx --test scripts/release-news/generate-indexes.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3",
    "yaml": "^2.7.1"
  }
}
